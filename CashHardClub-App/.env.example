# Copy to .env (never commit .env). The OneSignal App ID is a public identifier, not a secret;
# the REST API key stays in the OneSignal dashboard and must never be put in the app.
EXPO_PUBLIC_ONESIGNAL_APP_ID=
