# dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Store screenshots (raw captures + generated sets; uploaded to the consoles, not reviewed in git)
store/screenshots/

# Native (generated by `expo prebuild`; EAS Build regenerates them from app.json)
/ios
/android
.kotlin/
*.orig.*

# Signing material and store credentials — never commit these
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.pem
secrets/

# Metro / debug
.metro-health-check*
npm-debug.*
yarn-debug.*
yarn-error.*

# OS
.DS_Store
Thumbs.db

# local env files
.env
.env.*
!.env.example

# typescript
*.tsbuildinfo
